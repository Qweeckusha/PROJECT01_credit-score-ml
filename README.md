# Кредитный скоринг: классификация кредитоспособности клиентов

![ML Practice](https://img.shields.io/badge/ML%20Practice-PET%20Project-blue)
![Python](https://img.shields.io/badge/Python-3.13%2B-darkgreen)
![Scikit-learn](https://img.shields.io/badge/scikit--learn-1.8%2B-orange)
![License](https://img.shields.io/badge/License-MIT-lightgrey)

Проект по машинному обучению в рамках учебного курса. Цель — предсказать **категорию кредитного рейтинга** клиента (`Good`, `Standard`, `Poor`) на основе его финансового поведения и демографических данных.

Датасет: [Credit Score Classification Dataset (Kaggle)](https://www.kaggle.com/datasets/parisrohan/credit-score-classification?select=train.csv)

---

## 📌 Содержание

- [Описание задачи](#описание-задачи)
- [Установка и запуск](#установка-и-запуск)
- [Модели и результаты](#модели-и-результаты)
- [EDA и препроцессинг](#eda-и-препроцессинг)
- [Анализ ошибок и интерпретация](#анализ-ошибок-и-интерпретация)
- [Выводы и дальнейшие шаги](#выводы-и-дальнейшие-шаги)

---

## Описание задачи

Задача **многоклассовой классификации**: по данным о клиенте банка предсказать его кредитный рейтинг:
- `Poor` — низкий,
- `Standard` — средний,
- `Good` — высокий.

Используемые метрики:
- **F1-macro** (основная, учитывает баланс между классами),
- Accuracy,
- Precision / Recall по классам.

Практическая ценность: автоматизация оценки рисков при выдаче кредитов.

---

## Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Qweeckusha/PROJECT01_credit-score-ml.git
   cd PROJECT01_credit-score-ml
   ```

2. Создайте виртуальное окружение (рекомендуется):
   ```bash
   python -m venv venv
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Запустите Jupyter Notebook:
   ```bash
   jupyter notebook
   ```
   Откройте `classification-model.ipynb` и выполните все ячейки последовательно.

---

## Модели и результаты

Обучено и протестировано несколько моделей. Все метрики приведены на **тестовой выборке** (~30 000 записей).

### 1. На «грязных» данных (без глубокой очистки, с аномалиями)

| Модель                | F1-macro | Accuracy | Комментарий                     |
|----------------------|----------|----------|----------------------------------|
| Logistic Regression  | 0.538    | 0.60     | Baseline                         |
| Random Forest        | 0.763    | 0.78     | Без `class_weight`               |
| XGBoost              | —        | —        | Не обучался на этом этапе        |

> ⚠️ В данных присутствовали аномалии: отрицательный возраст, некорректные значения `Credit_History_Age`, пустые строки в `Type_of_Loan`.

### 2. После глубокой очистки и обработки аномалий

| Модель                | F1-macro | Accuracy | Комментарий                     |
|----------------------|----------|----------|----------------------------------|
| Logistic Regression  | 0.594    | 0.64     | Baseline                         |
| Random Forest        | **0.770**| **0.78** | Лучшая модель (`class_weight='balanced'`) |
| XGBoost              | 0.678    | 0.70     | Без тонкой настройки гиперпараметров |

> ✅ **Random Forest показал наилучшее качество** и стал финальной моделью.  
> Улучшение F1-macro на ~0.007 после чистки говорит о том, что обработка аномалий повысила стабильность модели.


---

## EDA и препроцессинг

Ключевые шаги:
- Обработка пропусков в числовых признаках (`Monthly_Inhand_Salary`, `Num_of_Delayed_Payment` и др.).
- Коррекция аномалий (например, отрицательный возраст → замена медианой).
- Преобразование `Type_of_Loan`: развернуто в 8 бинарных признаков (Auto Loan, Mortgage и т.д.).
- Кодирование категориальных переменных (`Occupation`, `Payment_Behaviour`) через `OneHotEncoder`.
- Удаление колонок без предиктивной силы (`ID`, `Customer_ID`, `SSN`, `Name`).
- Разделение на train/test **до** любой обработки (во избежание утечек).
- Генерация новых признаков `Debt_to_Income_Ratio` и `is_new_customer`

Выводы из EDA:
- Самая сильная корреляция между `Delay_from_due_date` и целевой переменной.
- Многие признаки коррелируют с `Monthly_Inhand_Salary`. Важный признак в теории
- Класс `Standard` доминирует (~53%), `Good` — наименьший (~18%). Дисбаланс классов
- Признак `Delay_from_due_date` критичен для различения `Poor` vs `Good`.

---

## Анализ ошибок и интерпретация

- **Feature Importance** (Random Forest): ключевые признаки — `Outstanding_Debt`, `Interest_Rate`, `Delay_from_due_date`, `Credit_History_Age`.
- Наиболее частые ошибки:
  - `Good` → `Standard`: клиенты с хорошей историей, но высокой долговой нагрузкой.
  - `Poor` → `Standard`: редкие платежи, но без явных просрочек в данных.
- Confusion Matrix показывает сбалансированное поведение модели после применения `class_weight='balanced'`.

---

## Выводы и дальнейшие шаги

✅ Проект успешно решает задачу классификации кредитного рейтинга с F1-macro ≈ **0.77**.

Что можно улучшить:
- Гиперпараметрическая оптимизация XGBoost.
